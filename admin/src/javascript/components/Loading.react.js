/* eslint-disable semi */
import * as React from 'react';

var Loading = React.createClass({
  render: function() {
    return (
      <div className="row" style={{position: 'absolute', 'left': '50%'}}>
      </div>
    );
  }
});

export default Loading;
